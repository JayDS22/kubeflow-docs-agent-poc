apiVersion: v1
kind: Service
metadata:
  name: docs-agent-server
  namespace: docs-agent
spec:
  selector:
    app: docs-agent-server
  ports:
  - name: api
    port: 8000
    targetPort: 8000
  - name: mcp
    port: 8001
    targetPort: 8001
  type: ClusterIP
